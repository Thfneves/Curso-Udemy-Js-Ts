//funcoes executam acoes
//geralmente e usada para mandar valor e pegar um valor.
//Para criar uma funcao a mais comun e function
function saudacao(nome) {
  return `Boom dia ${nome}!`;
}

// quando chamamos uma funcao precisamos chamar ela de volta por isso suamos saudacao()
// Podemos criar parametros para chamar ou alterar funcoes,
//funcoes e o que ficam dentro das chaves {}
const variavel = saudacao("luis");
console.log(variavel);
// essa funcao ela pode ser reutilizada quando quisermos
//usando return, para a funcao retornar um valor.
function soma(x, y) {
  let x = 10;
  let y = 5;

  return resultado;
  console.log(resultado);
}
